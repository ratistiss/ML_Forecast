Machine Learning model that uses multiple linear regression to predict the daily Hi temperature on Mars. This Model was built and trained in Jupyter Notebook, then put in a Pikl file which is used by Flask to take incoming data and make the prediction. 
